import volumeQty from "./volumeQty";
import allVolumes from "./allVolumes";
import volumeById from "./volumeById";

const volumeQueries = {
  volumeQty,
  allVolumes,
  volumeById,
};

export default volumeQueries;
